function postCreate()
    if(boyfriend.curCharacter == 'pico'){
        boyfriend.cameraOffset.set(-300, 0);
        dad.cameraOffset.set(-40, 0); //ill wait for changing his character camera offset cuz it would break everything and i dont want to deal with that for now :sob:
    }